# Set to 1 to enable production mode (affects logging and potentially other behaviors)
PRODUCTION=1

# Note: Other configurations like ports and Minecraft version are currently 
# managed in config.ts, but can be moved here in the future if needed.
